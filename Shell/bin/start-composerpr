#! /bin/bash

COMPOSER=`which composer`
GIT=`which git`
DIR=$1

mkdir ./"$DIR"

git clone git@github.com:jascha030/composer-template.git ./"$DIR"

cd ./"$DIR"
rm -rf .git/

$GIT init
$GIT flow init
$GIT add .gitignore && git commit -m"initial commit with composer-template"

